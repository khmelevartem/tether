{
  "hooks": {
    "worktreeCreate": {
      "description": "Copy local.properties from repo root to new worktree",
      "command": "bash -c 'MAIN_ROOT=$(dirname $(git rev-parse --git-common-dir)); cp \"$MAIN_ROOT/local.properties\" local.properties'"
    }
  }
}
